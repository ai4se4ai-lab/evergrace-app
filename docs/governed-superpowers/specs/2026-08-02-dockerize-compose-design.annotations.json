{
  "1": {
    "source": "existing_codebase",
    "ref": "prisma/schema.prisma:19-21",
    "text": "It currently runs on SQLite (`prisma/dev.db`) with schema changes applied via `prisma db push` — no versioned migrations exist yet."
  },
  "2": {
    "source": "human",
    "ref": null,
    "text": "The task is to containerize the app and stand it up via `docker compose`, aligned to `docs/DEV/Robust-Application-Development-Guidelines.md` (the companion checklist file is present but empty, so guidance is drawn from the guidelines doc alone)."
  },
  "3": {
    "source": "ai_assumption",
    "ref": null,
    "text": "`app` depends on `db` with a healthcheck gate so the container waits for a real, connectable database rather than just an open port."
  },
  "4": {
    "source": "existing_codebase",
    "ref": "docs/DEPLOYMENT.md:8-15",
    "text": "Switch `prisma/schema.prisma`'s `datasource` provider from `sqlite` to `postgresql`, per the migration path already documented in `docs/DEPLOYMENT.md`."
  },
  "5": {
    "source": "human",
    "ref": null,
    "text": "A `migrate` service (same image as `app`, overridden command: `prisma migrate deploy`) runs once per `docker compose up`, depends on `db` being healthy, and is expected to exit 0. `app` sets `depends_on: migrate: condition: service_completed_successfully`, so it never starts against a half-migrated schema and never races another `app` replica to apply migrations if the stack is ever scaled (`docker compose up --scale app=N`)."
  },
  "6": {
    "source": "existing_codebase",
    "ref": "prisma/schema.prisma",
    "text": "Existing Prisma-level constraints (FKs, `@unique`, not-null) carry over unchanged."
  },
  "7": {
    "source": "ai_assumption",
    "ref": null,
    "text": "Requires adding `output: \"standalone\"` to `next.config.ts` so the runner stage only needs the minimal traced output, not the full `node_modules` tree."
  },
  "8": {
    "source": "skill_doc",
    "ref": "docs/DEV/Robust-Application-Development-Guidelines.md:51",
    "text": "the runner stage runs as a non-root user, and its `CMD` is `node server.js` directly — no migration logic lives in this stage; that's the `migrate` service's job."
  },
  "9": {
    "source": "skill_doc",
    "ref": "docs/DEV/Robust-Application-Development-Guidelines.md:53",
    "text": "No environment-specific values or secrets are baked into any image layer — everything is supplied at container-start time via `env_file`/compose `environment`."
  },
  "10": {
    "source": "skill_doc",
    "ref": "docs/DEV/Robust-Application-Development-Guidelines.md:61",
    "text": "`db`: `postgres:16-alpine`, named volume for data persistence, healthcheck via `pg_isready`, explicit `deploy.resources.limits` (cpu/mem), `restart: unless-stopped`."
  },
  "11": {
    "source": "ai_assumption",
    "ref": null,
    "text": "`app`: builds from the Dockerfile, `env_file: .env`, `DATABASE_URL` overridden to point at `db:5432` (compose's internal DNS), `depends_on: db: condition: service_healthy` and `migrate: condition: service_completed_successfully`, port `3000:3000`, explicit resource limits, `restart: unless-stopped`, and its own healthcheck against `GET /api/health`."
  },
  "12": {
    "source": "skill_doc",
    "ref": "docs/DEV/Robust-Application-Development-Guidelines.md:58",
    "text": "The app container is stateless — no state that must survive a restart is written to its local filesystem; everything durable lives in Postgres."
  },
  "13": {
    "source": "human",
    "ref": null,
    "text": "`prisma/seed.ts` is already documented and written to be idempotent (upserts by natural key), so running it more than once is safe — it's kept manual/on-demand rather than automatic purely so a restart doesn't unexpectedly reseed reference data mid-demo, not because a second run would corrupt anything."
  },
  "15": {
    "source": "skill_doc",
    "ref": "docs/DEV/Robust-Application-Development-Guidelines.md:53",
    "text": "New `.dockerignore` excludes `.env`, `node_modules`, `.next`, `prisma/dev.db`, and other dev-only artifacts from the build context, so dev secrets can never leak into a built image layer."
  },
  "16": {
    "source": "skill_doc",
    "ref": "docs/DEV/Robust-Application-Development-Guidelines.md:74",
    "text": "Runner stage drops to a non-root user before running the app."
  },
  "17": {
    "source": "skill_doc",
    "ref": "docs/DEV/Robust-Application-Development-Guidelines.md:78",
    "text": "No secrets appear in the Dockerfile or `docker-compose.yml` themselves — only referenced via `env_file`."
  },
  "18": {
    "source": "ai_assumption",
    "ref": null,
    "text": "`.env.example` gains a documented Postgres `DATABASE_URL` shape for the Docker Compose setup (e.g. `postgresql://evergrace:evergrace@db:5432/evergrace`), alongside the existing SQLite default for non-Docker local dev."
  },
  "19": {
    "source": "human",
    "ref": null,
    "text": "Rate limiting, CDN/load balancing, multi-region HA/DR, and CI/CD pipeline changes are covered elsewhere in the guidelines doc but do not apply to a single-node local `docker compose` artifact — this is not a production topology change."
  },
  "20": {
    "source": "human",
    "ref": null,
    "text": "If `migrate` exits non-zero (bad migration, dropped connection mid-apply), it stays stopped/failed and `app` never starts — the failure is visible via `docker compose ps` / exit code, not masked by a partially-started app. `restart: \"no\"` on `migrate` (a one-shot job should never auto-restart in a loop on failure)."
  },
  "21": {
    "source": "human",
    "ref": null,
    "text": "installs npm dependencies with `npm ci` (not `npm install`), for reproducible, lockfile-exact installs. All stages pin an explicit Node base image tag (e.g. `node:20-alpine`), never `node:latest`, so rebuilds are reproducible."
  },
  "22": {
    "source": "human",
    "ref": null,
    "text": "The app validates required env vars (`DATABASE_URL`, `APP_URL`, `AUTH_SECRET`, `CRON_SECRET`) at startup and fails fast with a clear error message if any are missing or malformed, rather than surfacing an opaque failure deep inside Prisma's connection code."
  },
  "23": {
    "source": "human",
    "ref": null,
    "text": "The app logs to stdout/stderr only (never to a file), so `docker compose logs` and any future centralized-logging driver work without extra plumbing, per the guidelines' structured/centralized logging emphasis."
  },
  "24": {
    "source": "human",
    "ref": null,
    "text": "Three services: `db`, `migrate`, `app`, on a single named bridge network (`evergrace-net`). `db` publishes no host port — it's reachable only from other services on that network; add a port mapping temporarily only if a developer needs a local DB client for debugging."
  },
  "25": {
    "source": "human",
    "ref": null,
    "text": "This is a readiness check (is the app + its DB dependency ready to serve traffic), and it's the one Docker uses for the `app` healthcheck in this spec. It is deliberately not used as a liveness check: if Postgres blips transiently, we don't want Docker restarting an otherwise-healthy `app` container over it. Splitting into `/api/health/live` vs `/api/health/ready` is a reasonable follow-up but is collapsed into the single endpoint for this iteration."
  },
  "26": {
    "source": "human",
    "ref": null,
    "text": "Populating that checklist and diffing this spec against it is flagged as a follow-up task that should happen before — or in parallel with — implementation, not silently deferred."
  }
}
